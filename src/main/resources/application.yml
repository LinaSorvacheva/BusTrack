spring:
  datasource:
    url: jdbc:h2:mem:bustrackdb
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    spring.jpa.database-platform: org.hibernate.dialect.H2Dialects
  h2:
    console.enabled: true